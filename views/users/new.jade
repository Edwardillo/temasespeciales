extends ../layout

block content
	h1.
		#{title}
	form#formAddUser(name="adduser",method="post",action="/users")
		p Name:
			input#inputName(type="text",name="name")
		p Message:
			input#inputMessage(type="text", name="message")
		p
			button#btnSubmit(type="submit") submit
