extends ../layout

block content
    h1.
        User ID #{user._id}

    p User: #{user.name}
    p Message: #{user.message}
    P Counter: #{user.counter}
    ul
      each c in user.comments
        li
          = "usuario: "
          = c.user
          = " comentario: "
          = c.comment
